import InfrastructureMap from "@/features/infrastructure-map/InfrastructureMap";

export default function App() {
  return (
    <main className="mx-auto min-h-screen w-full max-w-[1680px] px-[clamp(16px,2vw,28px)] py-[clamp(16px,2.6vw,28px)]">
      <InfrastructureMap />
    </main>
  );
}
